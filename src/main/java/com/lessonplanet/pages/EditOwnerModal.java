package com.lessonplanet.pages;

import org.openqa.selenium.WebDriver;

public class EditOwnerModal extends EditTeacherModal {

    public EditOwnerModal(WebDriver driver) {
        super(driver);
    }
}
